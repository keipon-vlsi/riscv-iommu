# Work Log 2026-04-28

## Session 1 (時刻未記録-08:54) — Mermaid 修正 & cheat_sheet.md 生成

### Goal / Topic
全モジュールカードの Mermaid プレビュー失敗を修正し、仕様書ビットフィールドのチートシートを作成する。

### Done
- `rv_iommu_cdw_pc.md` の stateDiagram-v2 遷移ラベルの `\n` を修正 (parse error 解消)
- `rv_iommu_cdw_pc.md` のデータフロー flowchart のエッジラベル `|...\n(...)|` を修正 (line 23 の `got 'PS'` エラー解消)
- 全 5 モジュールカードの Mermaid ブロックを修正 (iotlb, ds_if, prog_if, tw_sv39x4_pc, cdw_pc)
- `.claude/commands/create-module-card.md` に Mermaid 安全規則 6 項目を追加
- `doc/spec/cheat_sheet.md` を新規生成 (12 データ構造/レジスタのビットフィールド一覧)

### Files changed
- Modified: `doc/modules/translation_logic/rv_iommu_iotlb_sv39x4.md` (Mermaid: `[i]`→`_i`, `||`→`or`, `()`除去, subgraph タイトル修正)
- Modified: `doc/modules/ext_interfaces/rv_iommu_ds_if.md` (Mermaid: diamond `()` 除去, エッジラベル `\n` 修正)
- Modified: `doc/modules/ext_interfaces/rv_iommu_prog_if.md` (Mermaid: subgraph タイトル `()` 除去)
- Modified: `doc/modules/translation_logic/wrapper/rv_iommu_tw_sv39x4_pc.md` (Mermaid: ノードラベル `()` 除去)
- Modified: `doc/modules/translation_logic/cdw/rv_iommu_cdw_pc.md` (stateDiagram `\n` 修正 + flowchart エッジラベル `\n`+`()` 修正)
- Modified: `.claude/commands/create-module-card.md` (Mermaid 安全規則セクション追加)
- Created: `doc/spec/cheat_sheet.md` (12 構造体のビットフィールド チートシート)

### Decisions / Findings
- Mermaid flowchart のエッジラベル `|...|` 内の `\n` は literal なソース改行として解釈され、直後の `(` が形状指定トークン `PS` として誤解析される → `\n` と `()` を両方除去する必要あり
- stateDiagram-v2 の遷移ラベルでも同様に `\n` が使えない
- `cheat_sheet.md` の `09-chapter-6` は 25000 トークン超で一括読み込み不可 → `offset`/`limit` チャンク読みで対処
- Non-leaf DDTE の PPN は `[53:10]`（44 bit）であることを仕様から確認（元カードの `[63:10]` は誤り）

### Open questions / TODO
- [ ] `doc/modules/translation_logic/rv_iommu_iotlb_sv39x4.md` の Non-leaf DDTE PPN 範囲表記 (`[63:10]` vs `[53:10]`) を RTL と照合して確認
- [ ] `doc/spec/cheat_sheet.md` の DC.msiptp フィールドの PPN ビット範囲が `[43:0]` ではなく `[55:4]` かどうかを仕様で再確認
- [ ] `tb_coco/translation_logic/ptw/` 以下の旧ファイル削除を git でコミット整理

### Next session
`doc/spec/cheat_sheet.md` の内容をレビューし、続いて `/create-module-card` または `/create-tb` で次モジュールの作業に着手する。

---

## Session 2 (時刻未記録-18:26) — モジュールカード生成 / 信号名解説 / begin-end 検証

### Goal / Topic
`rv_iommu_regmap` のモジュールカード生成、RTL 信号名規則の解説、`rv_iommu_tw_sv39x4_pc.sv` の begin/end 対応確認。

### Done
- `doc/spec/cheat_sheet.md` を新規生成 (12 データ構造/レジスタのビットフィールド一覧: device_id, DC, PC, Sv39 VA/PA/PTE, capabilities, fctl, ddtp)
- `/create-module-card rtl/software_interface/regmap/rv_iommu_regmap.sv` を実行し `doc/modules/software_interface/regmap/rv_iommu_regmap.md` を生成 (3550行 RTL 解析、BR01-BR16)
- `rv_iommu_field` モジュールの `_qs`/`_wd`/`_we`/`_d`/`_de` サフィックス規則を解説 (SW/HW 2経路 + SwAccess 属性による組み合わせ)
- `rv_iommu_tw_sv39x4_pc.sv` の `begin : translation` ブロックの begin/end 対応を Python スクリプトで自動検証 → 正しく対応していることを確認
- トップモジュールのスモークテスト用 TB 修正が行われコミット済み (50bc38b)

### Files changed
- Created: `doc/spec/cheat_sheet.md` (12構造体ビットフィールドチートシート)
- Created: `doc/modules/software_interface/regmap/rv_iommu_regmap.md` (モジュールカード、BR16個)
- Modified (user commit): `tb_coco/test/_example/iommu_smoke/test_smoke.py` (スモークテスト修正)
- Modified (user commit): `tb_coco/test/_example/iommu_smoke/tb_riscv_iommu_wrapper.sv` (ラッパ整理)
- Modified (uncommitted): `rtl/translation_logic/cdw/rv_iommu_cdw_pc.sv`
- Modified (uncommitted): `rtl/translation_logic/wrapper/rv_iommu_translation_wrapper.sv`
- Modified (uncommitted): `rtl/translation_logic/wrapper/rv_iommu_tw_sv39x4_pc.sv`

### Decisions / Findings
- `rv_iommu_field` の信号サフィックス: `_qs`=FF現在値(SW読み出し用), `_wd`/`_we`=SW書き込みパス, `_d`/`_de`=HW書き込みパス。capabilities フィールドは FF 不使用の純組合せ論理のため `_qs` のみ。
- `begin : translation` / `end : translation` の begin/end 対応は数学的に完全正確 (depth 0 で閉じることを Python で確認)。エディタのフォールドズレはラベル付き begin/end を正しく解析できない拡張機能の問題。
- `ddtp` 書き込みは 2-half ステージングプロトコル: Low(0x010)+High(0x014) の両方が書かれるまで commit しない。`iommu_mode > 4` の書き込みはサイレントドロップ。

### Open questions / TODO
- [ ] `rtl/translation_logic/cdw/rv_iommu_cdw_pc.sv` 等の未コミット変更の内容確認
- [ ] `fctl.gxl` に IOMMU Off ガードが無い点の検証 (§12.2 要検証項目)
- [ ] ddtp pending フラグが 2nd half 未受信のまま残存するケースの検証
- [ ] `rv_iommu_regmap` のテストプラン・TB 作成

### Next session
未コミットの RTL 変更内容を確認し、必要に応じてモジュールカードを更新する。次のテスト対象モジュール (`/create-tb` 対象) を決定する。

---
