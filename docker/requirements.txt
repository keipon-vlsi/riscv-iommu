# =============================================================================
# Python deps for the riscv-iommu cocotb testbench
# Pin everything — DUT 検証は再現性が命。
# =============================================================================

cocotb==2.0.0
cocotbext-axi==0.1.28

# テストベンチ補助
pytest==8.3.3
pyyaml==6.0.2
# pyuvm は将来 UVM-style に書き換えるとき用。今は不要なので install しない。
# 必要になったらコンテナ内で `pip install pyuvm==4.0.1` (or 2.9.1) で追加可能。

# 波形・カバレッジ後処理
matplotlib==3.9.2
numpy==2.1.3
pandas==2.2.3